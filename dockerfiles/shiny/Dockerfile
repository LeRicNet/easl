# syntax=docker/dockerfile:1
FROM rocker/shiny:latest
RUN apt-get update && apt-get install -y \
    libgsl-dev \
    libz-dev \
    libbz2-dev \
    liblzma-dev \
    libgeos-dev \
    libxml2-dev \
    libglpk-dev \
    libxt6 \
    libmagick++-dev